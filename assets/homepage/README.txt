HOWEBZ OPTIMIZED ASSETS

Copy all PNG files directly into your existing assets folder.
Overwrite the older files with the same names.
The filenames remain unchanged, so no HTML or CSS changes are required.
